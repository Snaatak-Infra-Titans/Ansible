---
- name: Update APT package cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install Redis
  ansible.builtin.apt:
    name: "{{ redis_package }}"
    state: present
